let input = ``

function one(){
    let lines=input.split(/[\n\r]/gm)
    let object = {}
    lines.forEach(line=>{
        [...line].forEach(char=>{

        })
    })

    let count=0
    console.log(count)
}

one()
two()